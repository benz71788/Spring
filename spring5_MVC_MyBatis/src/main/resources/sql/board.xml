<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
	<select id="getBoardList" resultType="Board">
		<![CDATA[
			select * from (select rownum rnum, BOARD_NUM, BOARD_NAME, BOARD_SUBJECT, BOARD_CONTENT, 
				BOARD_FILE, BOARD_RE_REF, BOARD_RE_LEV, BOARD_RE_SEQ, BOARD_READCOUNT, BOARD_DATE 
				from (select * from BOARD5 order by BOARD_RE_REF DESC, BOARD_RE_SEQ ASC))
		]]>
	</select>
	
	<insert id="boardInsert" parameterType="board">
		INSERT INTO BOARD5 (BOARD_NUM, BOARD_NAME, BOARD_PASS,
			BOARD_SUBJECT, BOARD_CONTENT, BOARD_RE_REF, BOARD_RE_LEV,
			BOARD_RE_SEQ, BOARD_READCOUNT, BOARD_DATE)
			VALUES(BOARD5_NUM_SEQ.NEXTVAL, #{board_name}, #{board_pass}, #{board_subject},
				#{board_content}, BOARD5_NUM_SEQ.NEXTVAL, 0, 0, 0, SYSDATE)
	</insert>
</mapper>






















